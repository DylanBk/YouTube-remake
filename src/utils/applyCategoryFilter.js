import React from "react";
const colors = require('../styles/colors')


// load videos based on category, database with col category
function applyCategoryFilter(category) {
    {}
}

export default applyCategoryFilter;